# 개발/테스트 전용 의존성 (런타임 이미지에는 포함하지 않음).
#   설치:  pip install -r requirements.txt -r requirements-dev.txt
# 핀이 아닌 범위를 쓰는 이유: 테스트 보조 도구는 런타임만큼 엄격한 재현성이 필요 없고,
# 3.11~3.13 모두에서 동작해야 하므로(로컬 검증은 3.13, CI/Docker 는 3.11).
pytest>=8,<9
fakeredis>=2.21,<3      # Redis 를 메모리로 대체(네트워크 없는 단위 테스트)
